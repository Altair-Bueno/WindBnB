const config = {
  reservas: {
    basePath: import.meta.env.RESERVAS_BASE_PATH,
  },
  viviendas: {
    basePath: import.meta.env.VIVIENDAS_BASE_PATH,
  },
  datosAbiertos: {
    basePath: import.meta.env.DATOSABIERTOS_BASE_PATH,
  },
};

export default config;
